 🐍 Day 11 – Math & Random in NumPy

 📚 Topics Covered:
- np.mean()  
- np.std()  
- np.random()  

🧩 1. np.mean()

`np.mean()` is used to calculate the **average** value of the elements in an array.  
It adds all the values and divides by the number of elements.

Useful in:
- Statistics  
- Data analysis  
- Machine learning feature scaling  

 ⚡ 2. np.std()

`np.std()` calculates the **standard deviation**, which measures how spread out the values are.

- Low std → values are close together  
- High std → values are more spread out  

Used in:
- Normalization  
- Probability  
- Statistical modeling  

 🎲 3. np.random()

`np.random` is a module that generates **random numbers**.

Common uses:
- Generating random integers  
- Creating random arrays  
- Simulating data  
- Shuffling  
- Machine learning experiments (train/test split)  

Functions:
- `np.random.rand()` → random floats (0 to 1)  
- `np.random.randint()` → random integers  
- `np.random.randn()` → random values from normal distribution  
